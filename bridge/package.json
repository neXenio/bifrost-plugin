{
  "name": "bifrost-oauth-bridge",
  "version": "0.1.0",
  "description": "OAuth 2.1 resource-server bridge in front of a Bifrost MCP gateway: RFC 9728 metadata, Keycloak token validation, per-user virtual-key mapping.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "sync": "node src/sync-vk-map.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/http-proxy": "^11.1.2",
    "fastify": "^5.2.1",
    "jose": "^5.9.6"
  }
}
