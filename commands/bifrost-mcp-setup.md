---
description: Manually register the Bifrost MCP server when the automated setup can't run.
---

# /bifrost-mcp-setup

Manually wire the Bifrost MCP server into Claude Code when the automated installer
can't run.

Load and follow the **`bifrost-mcp-setup`** skill
(`skills/bifrost-mcp-setup/SKILL.md`). Natural-language triggers such as "manually
add bifrost mcp" or "edit mcp.json for bifrost" invoke the same skill.
